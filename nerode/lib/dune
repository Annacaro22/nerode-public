(ocamllex (modules rx_lexer cfg_lexer))
(menhir (modules rx_parser cfg_parser))

(env
  (dev
    (flags (:standard -warn-error -A))))

(library
  (name nerode)
  (public_name nerode)
  (libraries yojson nice_parser core))

(documentation
  (package nerode)
  (mld_files :standard))
